#pragma once

#include "Cities.h"

void input(Cities*&, int&, char*);
void add(Cities*&, int& amount, char*);
void output(Cities*, int);
void del(Cities*, int&, char*);
void fieldCorrection(Cities*&, int, char*);
